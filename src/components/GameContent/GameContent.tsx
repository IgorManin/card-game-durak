import React from 'react';

export const GameContent = () => {
  // const { cards } = useSelector((state: RootState) => state);

  return (
    <div>
      {/*<Player />*/}
      {/*<Deck />*/}
      {/*<Player />*/}
    </div>
  );
};
